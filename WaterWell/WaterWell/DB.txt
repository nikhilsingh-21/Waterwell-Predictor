create database waterwell;
use waterwell;

create table signup(username varchar(50), password varchar(50),
contact_no varchar(12),
email_id varchar(50),
address varchar(50));

create table feedback(username varchar(50), feedback varchar(500), nlp_feedback_analysis varchar(50));

